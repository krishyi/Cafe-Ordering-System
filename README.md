# Cafe-Ordering-System
Backend service that accepts natural language orders and returns a structured response with summary. The conversation supports multiple turns — customers can ask questions, add, remove, or modify items before finalizing their order.
